.版本 2

.程序集 体育比赛相似度

.子程序 _按钮_计算_被单击
.局部变量 主队, 文本型
.局部变量 客队, 文本型
.局部变量 分数, 整数型

主队 ＝ 到小写 (编辑框_主队.内容)
客队 ＝ 到小写 (编辑框_客队.内容)

.如果真 (主队 ＝ "" 或 客队 ＝ "")
    信息框 ("请输入主队和客队英文名！", 0, "提示")
    返回 ()
.如果真结束

分数 ＝ 计算队名相似度 (主队, 客队)
标签_结果.标题 ＝ "相似度：" ＋ 到文本 (分数) ＋ " / 100"

.子程序 计算队名相似度, 整数型
.参数 A, 文本型
.参数 B, 文本型
.局部变量 长度A, 整数型
.局部变量 长度B, 整数型
.局部变量 最短长度, 整数型
.局部变量 相同位置字符数, 整数型
.局部变量 i, 整数型
.局部变量 字符A, 文本型
.局部变量 字符B, 文本型
.局部变量 结果, 整数型

长度A ＝ 取文本长度 (A)
长度B ＝ 取文本长度 (B)

.如果真 (长度A ＝ 0 且 长度B ＝ 0)
    返回 (100)
.如果真结束

.如果真 (长度A ＝ 0 或 长度B ＝ 0)
    返回 (0)
.如果真结束

最短长度 ＝ 取最小值 (长度A, 长度B)
相同位置字符数 ＝ 0

.计次循环首 (最短长度, i)
    字符A ＝ 取文本中间 (A, i, 1)
    字符B ＝ 取文本中间 (B, i, 1)
    .如果真 (字符A ＝ 字符B)
        相同位置字符数 ＝ 相同位置字符数 ＋ 1
    .如果真结束
.计次循环尾 ()

' 使用类似 Dice 的公式，保证范围 0~100
结果 ＝ (相同位置字符数 × 200) ÷ (长度A ＋ 长度B)

.如果真 (结果 ＞ 100)
    结果 ＝ 100
.如果真结束

.如果真 (结果 ＜ 0)
    结果 ＝ 0
.如果真结束

返回 (结果)

.子程序 取最小值, 整数型
.参数 a, 整数型
.参数 b, 整数型
.如果真 (a ＜ b)
    返回 (a)
.否则
    返回 (b)
.如果结束
